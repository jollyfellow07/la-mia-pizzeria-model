@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<Pizza>
<main>
    <div class="container">
        <div class="row justify-content-center mx-5">

            @if(Model.Count() == 0)
            {
                <h3>Mi dispiace non ci sono Pizze!</h3>
            }
            else
            {
                @foreach(Pizza menu in Model)
                {
                    <div class="col-12 col-md-6 col-lg-4 text-center mt-3">
                            <div class="card shadow mb-5 bg-body rounded justify-content-center" style="width: 24rem;">
                           <!-- SLIDE -->
                            <div id="carouselExampleSlidesOnly" class="carousel slide" data-bs-ride="carousel">
                              <div class="carousel-inner">
                                <div class="carousel-item active">
                                  <img src="@menu.image"  height="350" class="d-block w-100" alt="...">
                                </div>
                                <div class="carousel-item">
                                  <img src=@menu.image2 height="350"  class="d-block w-100" alt="...">
                                </div>
                              </div>
                            </div>
                            <!--FINE SLIDE -->
                                <div class="card-body p-0 bg-opacity-50">
                                  <h5 class="card-title bg-warning text-white p-2">@menu.title</h5>
                                  <p class="card-text text-center p-3">@menu.ingredienti</p>
                                  <hr class="m-0" >
                                  <p class="text-warning p-0 m-0 fs-6" >ATTENZIONE: PIZZA CON GLUTINE </p>
                                  <p class="p-4"> <b>@menu.prezzo €</b></p>
                                  <p>
                                      <button type="button" class="btn btn-warning">Acquista</button>
                                      <span>     </span>
                                      <button type="button" class="btn btn-warning">Dettagli</button>
                                  </p>
                                </div>
                          </div>
                        </div>
                }
            }
        </div>
    </div>
</main>

                        